# Sprite Multi-Agent Configuration
# Empty agents configuration for sprite v0.3.2

version: "0.3.2"
project_name: "sprite-project"

# No agents configured
agents: []

# Default session settings
session:
  default_layout: "tiled"
  auto_cleanup: true
  session_timeout: 3600

# Global settings
settings:
  log_level: "info"
  max_concurrent_agents: 10
  health_check_interval: 30
